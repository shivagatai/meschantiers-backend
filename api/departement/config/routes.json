{
  "routes": [
    {
      "method": "GET",
      "path": "/departements",
      "handler": "departement.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/departements/count",
      "handler": "departement.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/departements/:id",
      "handler": "departement.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/departements",
      "handler": "departement.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/departements/:id",
      "handler": "departement.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/departements/:id",
      "handler": "departement.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
